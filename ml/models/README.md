# Models (offline artifacts)

This folder is for **offline** training artifacts and TensorFlow.js export outputs during development.

- Do not commit large binaries.
- Runtime inference remains **client-side** and will load a small TF.js model bundle from the frontend when integrated.

